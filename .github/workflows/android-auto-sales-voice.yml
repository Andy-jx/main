name: Build Android Auto Sales Voice

on:
  push:
    branches:
      - tool/android-auto-sales-voice
    paths:
      - "tools/android-auto-sales-voice/**"
      - ".github/workflows/android-auto-sales-voice.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35 SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build debug APK
        working-directory: tools/android-auto-sales-voice
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: auto-sales-voice-debug-apk
          path: tools/android-auto-sales-voice/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
